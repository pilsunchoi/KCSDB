# HTTP 호출
requests>=2.31.0

# 데이터 처리
pandas>=2.1.0
numpy>=1.26.0

# 데이터베이스 / 저장
duckdb>=0.9.0
pyarrow>=14.0.0

# XML 파싱 (관세청 API 응답)
lxml>=4.9.0

# 환경변수 / 설정
python-dotenv>=1.0.0
pyyaml>=6.0

# 진행률 표시
tqdm>=4.66.0

# 로깅 (기본 logging으로 시작, 필요시 추가)
# loguru>=0.7.0

# 분석 단계 (나중에 활성화)
# matplotlib>=3.8.0
# seaborn>=0.13.0
# statsmodels>=0.14.0
# linearmodels>=5.4

# 개발/테스트
pytest>=7.4.0
ipython>=8.18.0
jupyter>=1.0.0
